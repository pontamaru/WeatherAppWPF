<UserControl x:Class="WeatherAppWpf.Views.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	    xmlns:bh="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WeatherAppWpf.Views"
        mc:Ignorable="d"
        Height="300" Width="250"
        Background="White">
    <Border BorderBrush="Black" BorderThickness="2" CornerRadius="5">
        <StackPanel Margin="5" Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Left">
            <Label Content="設定地域" FontWeight="Bold"/>
            <ComboBox 
				x:Name="CityComboBox" 
                ItemsSource="{Binding CityComboBox}"
                SelectedItem="{Binding SelectedComboBoxItem}"
                Width="150" Height="20">
                <bh:Interaction.Triggers>
                    <bh:EventTrigger EventName="SelectionChanged">
                        <bh:InvokeCommandAction Command="{Binding SelectedItemCommand}" />
                    </bh:EventTrigger>
                </bh:Interaction.Triggers>
            </ComboBox>
        </StackPanel>
    </Border>
</UserControl>
