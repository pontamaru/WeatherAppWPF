<UserControl x:Class="WeatherAppWpf.Views.TodayWeatherView"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:local="clr-namespace:WeatherAppWpf.Views"
		xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
		mc:Ignorable="d"
		Height="{Binding ElementName=RootWindow, Path=ActualHeight}"
		Width="{Binding ElementName=RootWindow, Path=ActualWidth}"
		Background="White">
	<Border BorderBrush="Black" BorderThickness="2" CornerRadius="5">
		<StackPanel Margin="5">
			<!--日付-->
			<StackPanel Orientation="Horizontal">
				<Label Content="今日の天気" FontWeight="Bold"/>
				<Label Content="{Binding TodayWeatherInfo.DateAndWeek}"/>
				<Button Content="更新" Command="{Binding UpdateWeatherCommand}" Margin="10,0,0,0"/>
			</StackPanel>
			<Label x:Name="TargetCity" Content="{Binding TargetCity}" FontWeight="Bold" FontSize="20"/>
			<Separator Background="LightGray" Height="1"/>
			<!--お天気情報-->
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="*"/>
					<RowDefinition Height="*"/>
					<RowDefinition Height="*"/>
					<RowDefinition Height="*"/>
				</Grid.RowDefinitions>
				<svgc:SvgViewbox Source="{Binding TodayWeatherInfo.WeatherIcon}" HorizontalAlignment="Left"  Height="50" Margin="20,10,0,5" Grid.Row="0"/>
				<Label Content="{Binding TodayWeatherInfo.Weather}" FontWeight="Bold" FontSize="15" Grid.Row="1"/>
				<Label Content="{Binding TodayWeatherInfo.Temperature}" FontSize="13" Grid.Row="2"/>
				<Label Content="{Binding TodayWeatherInfo.RainProbability}" FontSize="13" Grid.Row="3"/>
			</Grid>
		</StackPanel>
	</Border>
</UserControl>
	
