<UserControl x:Class="WeatherAppWpf.Views.ThreeDaysWeatherView"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:local="clr-namespace:WeatherAppWpf.Views"
		xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
		mc:Ignorable="d"
		Height="{Binding ElementName=RootWindow, Path=ActualHeight}"
		Width="{Binding ElementName=RootWindow, Path=ActualWidth}"
		Background="White">
	<UserControl.Resources>
		<!-- 1日分の天気情報テンプレート -->
		<DataTemplate x:Key="WeatherInfoTemplate">
			<StackPanel Orientation="Vertical">
				<Label Content="{Binding DateAndWeek}"/>
				<StackPanel Orientation="Horizontal">
					<svgc:SvgViewbox Source="{Binding WeatherIcon}" Height="50" Width="80"/>
					<StackPanel Orientation="Vertical">
						<Label Content="{Binding Weather}" FontWeight="Bold"/>
						<Label Content="{Binding Temperature}"/>
						<Label Content="{Binding RainProbability}"/>
					</StackPanel>
				</StackPanel>
				<Separator Background="LightGray" Height="1"/>
			</StackPanel>
		</DataTemplate>
	</UserControl.Resources>
	<Border BorderBrush="Black" BorderThickness="2" CornerRadius="5">
		<Grid Margin="5">
			<Grid.RowDefinitions>
				<RowDefinition Height="26"/>
				<RowDefinition Height="35"/>
				<RowDefinition Height="10"/>
				<RowDefinition Height="*"/>
			</Grid.RowDefinitions>
			<!--日付-->
			<StackPanel Orientation="Horizontal" Grid.Row="0">
				<Label Content="3日間の天気" FontWeight="Bold"/>
				<Button Content="更新" Command="{Binding UpdateWeatherCommand}" Margin="10,0,0,0"/>
			</StackPanel>
			<Label x:Name="TargetCity" Content="{Binding TargetCity}" FontWeight="Bold" FontSize="20" Grid.Row="1"/>
			<Separator Background="LightGray" Height="1" Grid.Row="2"/>
			<!--お天気情報-->
			<ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Grid.Row="3">
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="*"/>
					</Grid.RowDefinitions>
					<StackPanel Orientation="Vertical" Grid.Row="0">
						<ItemsControl ItemsSource="{Binding ThreeDaysWeatherInfo}" ItemTemplate="{StaticResource WeatherInfoTemplate}"/>
					</StackPanel>
				</Grid>
			</ScrollViewer>
		</Grid>
	</Border>
</UserControl>
